import 'package:flutter/material.dart';

class MaintenanceUnitsView extends StatelessWidget {
  const MaintenanceUnitsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Maintenance Units')),
    );
  }
}
